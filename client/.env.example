# Google Maps JavaScript API key for the territory map.
# Locally: copy this file to .env.local. On Vercel: add it as an Environment
# Variable (it's baked in at build time, so redeploy after adding).
VITE_GOOGLE_MAPS_API_KEY=
